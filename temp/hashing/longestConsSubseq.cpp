/*IDEA: insert all elements in a hash set
        for each element check if its starting element of a subsequence we do that by checking if the element before it is present
        if yes we keep incrementing till we do not find a discrepency
        else we move to the next element
              